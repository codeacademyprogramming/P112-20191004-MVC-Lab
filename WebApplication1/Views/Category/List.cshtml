@model List<WebApplication1.Models.Category>
@{
    ViewBag.Title = "Main";
}

<h2>Kateqoriyalar</h2>

<ul>
    @for (int i = 0; i < Model.Count; i++)
    {
        <li>@Model[i].Name</li>
    }
</ul>

